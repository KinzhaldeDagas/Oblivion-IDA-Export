int sub_9F1420()
{
  GameSetting_ConstrAndReg(
    (int *)&unk_B387F0,
    (int)"sNoDeviceSelected",
    (int)"You have not selected a storage device.  You will need to select one before you can save.  Until then, Autosave is disabled.");
  return atexit(sub_A21700);
}
