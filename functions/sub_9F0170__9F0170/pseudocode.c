int sub_9F0170()
{
  GameSetting_ConstrAndReg(
    &dword_B38348,
    (int)"sLevelUp11",
    (int)"Being smart doesn't hurt. And a little luck now and then is nice. But the key is patience and hard work. And wh"
         "en it pays off, it's SWEET!");
  return atexit(sub_A20DB0);
}
