Request addressed: "Browse the repo for all repo hooks for SpeedTree".

Interpretation used:
1) Inventory all functions explicitly marked as hooks in the exported repo index.
2) Check whether any hook-named function is SpeedTree-related.
3) Collect SpeedTree-specific symbols and callsites that could be practical hook targets.

Key query commands run:
- rg -n --ignore-case "speedtree.*hook|hook.*speedtree" .
- python scan of functions_index_by_name.json and functions_index_by_ea.json for entries with "hook" in name/display_name.
- python scan of metadata/strings.json for values containing "SpeedTree".

High-level result:
- No direct "SpeedTree"+"Hook" symbol exists in the repository export.
- Exactly two hook-named functions exist globally, and neither name indicates SpeedTree specificity.
