Questions answered and discoveries:

Q1) Are there explicit repository hooks that are clearly for SpeedTree?
A1) No direct SpeedTree hook symbol was found.
    - Pattern search for "speedtree.*hook|hook.*speedtree" returned no matches.

Q2) What hook-named functions exist in the repository export?
A2) Exactly two hook-named functions were found in both function index files:
    - NiDX9ImplicitDepthStencilBufferDataEx_GetBufferDataHook
    - __initp_eh_hooks

Q3) If there are no explicit SpeedTree hooks, what SpeedTree interception points are visible?
A3) SpeedTree-related runtime method references are present via CSpeedTreeRT strings and nearby callsites.
    Candidate operational targets (inferred from string-labelled call contexts):
    - SetLeafLightingMethod
    - NotifyAllTreesOfEvent
    - SetTime
    - MakeInstance
    - LoadTree

Conclusion:
- Current export appears to include broad hook infrastructure entries, but not a named dedicated "SpeedTree hook" function.
- SpeedTree integration points are still strongly represented through symbol names, shader/property classes, and CSpeedTreeRT method-labelled callsites.
